export enum GROUP {
  ASSETS = "Assets",
  OVERVIEWS = "Overviews",
  MODELS = "Models"
}

export enum MIME_TYPES {
  IMAGE = "image/*",
  DOCX = "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
  PDF = "application/pdf"
}
